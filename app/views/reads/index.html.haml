%p#new_read= link_to "Enter a Read", new_read_path
- if @reads
  %ul#read_list
    - for read in @reads
      %li= "#{read.read_date}: #{read.issue.comic.title} ##{read.issue.issue_num}"
  = will_paginate @reads
